{
  "name": "enigma",
  "version": "0.0.0",
  "description": "A very fast, stable parser",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc src/*.ts --outDir dist",
    "test": "mocha test/internal/**",
    "test:fast": "mocha test/internal/** --grep '\\(slow\\)' --invert"
  },
  "author": "Isiah Meadows",
  "license": "ISC",
  "devDependencies": {
    "@types/mocha": "^2.2.41",
    "clean-assert": "^1.0.0",
    "mocha": "^3.3.0",
    "ts-node": "^3.0.4",
    "tslint": "^5.2.0",
    "typescript": "^2.3.2",
    "unicode-10.0.0": "^0.7.4"
  }
}
